# 曜日別授業日設定・授業設定UI改修 実装仕様（Terra引継ぎ用）

- 仕様確定日: 2026-09-06
- 状態: 実装・公開確認済み
- 対象: TaskKanri
- この文書は、実装前に確定した仕様と受け入れ条件を記録する

## 1. 目的

学校ごとの通常週として、土曜授業や平日の固定休業日を曜日単位で設定できるようにする。

同時に、授業設定周辺の分かりにくい操作を整理する。ただし、保存済みの表示コマ、チャイム、基本時間割、期間別の基本時間割規則、日別変更、休日、日種別を削除・推測変更しない。意図した仕様変更以外の既存挙動を変えないことを受け入れ条件とする。

## 2. 用語

- **授業日**: 通常週に1〜7限を表示し、チャイム時刻を表示する曜日。
- **固定休業日**: 曜日設定で「授業日」をオフにした曜日。毎週の基準設定であり、日付固有の休日とは別に保存する。
- **休日・祝日**: `customHolidays` に保存された日付固有の休日。既存仕様を維持する。
- **日種別**: `dayProfiles` の短縮、短縮AM、午前時程、考査、模擬試験、授業なしの各設定。
- **授業枠**: 1〜7限。
- **非授業枠**: 朝、昼休み、放課後。

UIでは曜日設定を「授業日／固定休業日」、日付固有の設定を「休日・祝日」と呼び分ける。

## 3. 確定した利用者向け仕様

### 3.1 曜日ごとの授業日

設定画面の「曜日ごとの表示コマ ＆ チャイム設定」を「授業日・表示コマ・チャイム設定」に変更し、各曜日に「授業日」チェックボックスを追加する。

- チェックあり: 授業日。
- チェックなし: 固定休業日。
- 曜日の並びは現状どおり月、火、水、木、金、土、日。
- 設定は年度ではなくアプリの通常週設定として保持し、年度変更では初期化しない。

### 3.2 固定休業日の表示

日種別が「通常」である固定休業日は、次のように表示する。

- 1〜7限は表示しない。
- すべてのチャイム時刻を表示しない。
- 朝、昼休み、放課後は `daySlotConfig` の設定どおり表示する。
- 朝、昼休み、放課後の名称は変更しない。
- 予定、基本時間割、期間別規則、日別変更、表示コマのチェック状態は削除しない。
- 再び授業日にすると、保持されていたコマ設定と予定をそのまま復元する。
- 時数マネージャーでは1〜7限を除外し、理由を「固定休業日のため除外」とする。
- 朝、昼休み、放課後は現在どおり時数マネージャーの授業数対象外である。

日曜の `daySlotConfig` がすべてオフであれば何も表示されない。これは「非授業枠を強制表示する」のではなく、利用者が設定した表示コマを尊重するための意図した挙動である。

### 3.3 日付固有設定の優先順位

ユーザー確認済みの方針として、日付固有の日種別を固定曜日設定より優先する。優先順位は次のとおり。

1. `dayProfiles[dateId]` が `normal` 以外なら、その日種別の既存挙動を適用する。
2. 日種別が `normal` の場合、日付固有の有効な休日・祝日を適用する。
3. 休日・祝日でなければ、曜日の授業日／固定休業日設定を適用する。
4. 表示対象となった枠の中で `daySlotConfig` を適用する。

具体例:

| 固定曜日 | 日付固有設定 | 結果 |
|---|---|---|
| 授業日 | 通常 | 現在の通常授業日 |
| 固定休業日 | 通常 | 1〜7限とチャイムを隠し、朝・昼休み・放課後は設定どおり表示 |
| 固定休業日 | 短縮／短縮AM／午前時程 | その日だけ指定時程として表示 |
| 固定休業日 | 考査／模擬試験 | その日だけ指定考査として表示 |
| 固定休業日 | 授業なし（枠を隠す／表示） | 既存の各「授業なし」動作をそのまま適用 |
| 固定休業日かつ休日 | 通常 | 休日表示を適用 |
| 固定休業日かつ休日 | 通常以外の日種別 | 日種別を優先し、休日設定と固定曜日設定は保存したままにする |

日種別を「通常に戻す」と、その日付に残っている休日・祝日が再び有効になる。休日でなければ固定曜日設定に戻る。これは既存の「休日設定を保持したまま日種別を優先する」挙動を拡張するものである。

### 3.4 初期表示日の選択

起動時に選ばれる日付は、現在の土日固定判定を廃止し、上記の有効な日付判定を使う。

- 日種別が通常の固定休業日は飛ばす。
- 有効な休日・祝日は飛ばす。
- 固定休業日でも通常以外の日種別が設定されていれば飛ばさない。
- 平日の「授業なし」は現状どおり起動候補から除外しない。この挙動は本改修では変えない。

表示日の進む／戻る操作、左カレンダーからの選択、検索結果からの選択には、従来どおり日付選択の制限を加えない。

### 3.5 日付上の表示

固定休業日である理由が分かるよう、日種別が通常で固定休業日が有効な日には文字の状態表示を付ける。

- メイン編集枠の見出し: `固定休業日` の小さなテキストバッジ。
- 左の日付一覧および日付一括設定カレンダー: `休` のテキストバッジ。`title` と `aria-label` は `固定休業日`。
- 休日・祝日なら既存の休日表示を優先する。
- 短縮、考査、授業なし等の日種別なら既存の日種別表示を優先する。
- 土曜の青、日曜・祝日の赤というカレンダー色は授業日の有無と分離し、変更しない。
- 状態は色だけで表現しない。

## 4. 設定画面UI

### 4.1 セクション2

見出しと保存ボタンを次に変更する。

- 見出し: `2. 授業日・表示コマ・チャイム設定`
- 保存ボタン: `授業日・表示コマ・チャイムを保存`

セクション内を次の二つのまとまりに分ける。

1. `曜日ごとの授業日と表示コマ`
2. `チャイム時刻`

横幅に余裕がある場合は横並びを許可し、狭い画面では縦並びにする。表だけが極端に縮まないよう、必要な場合は表示コマ側を横スクロール可能にする。

表示コマ表は次の列構成とする。

`曜日 | 授業日 | 朝 | 1 | 2 | 3 | 4 | 昼 | 5 | 6 | 7 | 放`

行の仕様:

- 曜日は `th scope="row"` とする。
- 授業日チェックボックスには例として `水曜日を授業日にする` の `aria-label` を付ける。
- チェック状態に加えて、行内に `授業` または `休業` の文字を表示する。
- 固定休業日にすると1〜7限のチェックボックスを無効化・減色するが、DOM上のチェック状態と保存値は変えない。
- 固定休業日でも朝、昼休み、放課後のチェックボックスは操作可能とする。
- チェックを授業日に戻すと、1〜7限のチェックボックスを元の状態のまま再び操作可能にする。
- 現在は曜日名クリックで全コマが反転するが、クリック可能であることが分からない。この隠れ操作は廃止し、各曜日に明示的な `表示枠を全反転` ボタンを設ける。
- `表示枠を全反転` は既存機能を保持するため10枠すべてを反転する。固定休業日中の無効な1〜7限も保存値として反転し、チェック表示で結果を確認できるようにする。

説明文は次の内容を含める。

> 固定休業日は1〜7限とチャイムを表示しません。朝・昼・放課後は表示コマ設定に従います。予定やコマ設定は削除されず、授業日に戻すと復元されます。

チェックボックス群は視覚的かつセマンティックに曜日単位で関連付ける。ネイティブの `input type="checkbox"` を使い、色のみ、独自ARIAのみのチェックボックスにはしない。

### 4.2 セクション3「基本時間割」

ユーザー確認済みの方針として、各セル先頭の重複チェックボックスと `☑ 全てのチェックを反転` ボタンを削除する。表示コマの変更箇所はセクション2だけに統一する。

- `daySlotConfig` がオンのセル: 従来どおり編集可能。
- `daySlotConfig` がオフのセル: 入力を無効化し、既存の網掛けを表示する。ツールチップ／説明は `表示コマ設定で無効（保存済み内容は保持）`。
- 固定休業日であっても、`daySlotConfig` がオンなら基本時間割は編集できる。日付固有の日種別で授業日にした場合や、後から曜日を授業日に戻した場合に利用できる基礎データだからである。
- 固定休業日の列見出しには `休` を付け、`aria-label` を例として `水曜日・固定休業日` とする。
- セクション2の未保存のチェック変更は、同じ設定画面内のセクション3へ即時反映する。ただし永続化はセクション2の保存ボタンを押した時だけ行う。

基本時間割保存時のデータ保持を修正する。

- 編集可能セルは、入力値で `weeklyTemplate` を更新する。空欄ならそのセルだけ削除する。
- `daySlotConfig` により無効なセルは、現在の `weeklyTemplate` の値を変更・削除しない。
- `weeklyTemplate` 全体を空から作り直して無効セルを落としてはならない。
- 「この期間の基本時間割として適用」でも、無効セルの下書き・既存規則・日別変更を削除しない。
- 固定休業日だけを理由に基本時間割セルや期間適用対象を無効化しない。

### 4.3 保存と画面離脱

- 画面上部の `← 変更を適用して戻る` を `← メイン画面に戻る` に変更する。戻る操作自体は保存しない。
- 設定画面を開いた時点で、セクションごとの初期フォーム値を記録する。
- 入力変更のあったセクションだけを未保存として管理する。
- 各保存が成功した場合、そのセクションだけ未保存状態を解除する。
- 保存に失敗した場合は未保存状態を解除しない。
- 未保存項目がある状態で戻る場合は `保存していない変更があります。破棄してメイン画面に戻りますか？` と確認する。キャンセル時は設定画面に残る。
- `saveTimeConfig()` は保存後に設定画面を閉じない。他セクションの未保存入力を失わないためである。
- 元に戻す／やり直すを設定画面表示中に禁止する既存ガードは維持する。

## 5. データ仕様と移行

### 5.1 保存フィールド

`instructionDayConfig` をマスターstateへ追加する。

```json
{
  "0": false,
  "1": true,
  "2": true,
  "3": true,
  "4": true,
  "5": true,
  "6": false
}
```

- キー: JavaScriptの曜日番号。`0=日`、`1=月`、…、`6=土`。
- 値: boolean。`true=授業日`、`false=固定休業日`。
- 未知の曜日キー、boolean以外、オブジェクト以外は検証エラーにする。
- 一部の曜日だけを含む内部入力は既定値で補完してよいが、正規化後は0〜6をすべて持つ。
- `STATE_KEYS` に追加し、エクスポート、インポート、保存、undo/redoのstate全体で扱う。
- 旧ローカルストレージの個別キーは追加しない。

### 5.2 schema 5

- `APP_CONFIG.schemaVersion` を5にする。
- `supportedSchemaVersions` に5を追加する。
- schema 4→5の明示的な移行関数を `MIGRATION_REGISTRY` に追加する。
- schema 1〜4のデータには上記の既定 `instructionDayConfig` を追加する。
- schema 5に当該項目がない場合は `defaultState()` の既定値で補完する。
- schema 5のwrapped形式に不正な値があれば、既存と同じく保存前に拒否する。
- schema移行前の復旧snapshot作成、snapshot失敗時の読み取り専用化、現行raw保持という既存の安全動作を変更しない。
- 移行は入力オブジェクトを変更しない。

移行時に `daySlotConfig` から授業日を推測してはならない。現在の初期土曜日は1〜4限がオンである一方、起動日選択とチャイムは土日固定で休業扱いされているため、推測すると既存利用者の土曜日を意図せず授業日に変えてしまう。

既定値は月〜金を授業日、土日を固定休業日とする。既存の `daySlotConfig` は一切書き換えない。そのため、移行後に土曜日を授業日にチェックすると、従来保持されていた土曜の表示コマが復元される。

注意: 新しい固定休業日の確定仕様により、移行直後の土曜日は保持された1〜7限データが画面と時数集計から隠れる。これは「土曜日を固定休業日として扱う」ための意図した変更であり、データ削除ではない。チャイム非表示と起動時に土日を飛ばす挙動は従来どおりである。

## 6. 判定処理

土日判定を各画面へ直接書かず、`taskkanri-core.mjs` に純粋関数を追加して共通利用する。

推奨API:

```js
defaultInstructionDayConfig()
normalizeInstructionDayConfig(value)
getDateSchedulePolicy(dateId, state)
isSlotVisibleForDate(dateId, slot, state)
```

`getDateSchedulePolicy()` は少なくとも次を返す。

```js
{
  weekday,
  profile,
  hasExplicitProfile,
  isConfiguredInstructionDay,
  isFixedOffActive,
  isEffectiveHoliday,
  hidePeriodSlots,
  hideChime,
  countExclusionReason
}
```

判定規則:

- `hasExplicitProfile = profile !== 'normal'`。
- `isFixedOffActive = !hasExplicitProfile && !instructionDayConfig[weekday]`。
- 休日は既存の `isEffectiveHoliday()` と同様、通常以外の日種別がある間は無効表示とし、データは保持する。
- `hidePeriodSlots = isEffectiveHoliday || isFixedOffActive`。`noclass-hide` 等の日種別固有の隠し方は既存処理を別途そのまま適用する。
- `hideChime = isEffectiveHoliday || isFixedOffActive`。日種別が優先された場合のチャイムは既存の日種別動作を変えない。
- `countExclusionReason` は固定休業日、休日、午前時程、表示コマ設定の順ではなく、利用者に最も直接的な除外原因を一つだけ返す。固定休業日が有効なら `固定休業日のため除外`、有効な休日なら既存の `休日・祝日のため除外` を優先する。

`isWeekend()` はカレンダー色など純粋な土日判定に必要なら残してよいが、授業の有無、チャイム、起動日、時数除外の判断には使わない。

共通判定を少なくとも次へ適用する。

- メイン／サブ編集枠の表示コマとチャイム。
- 左の日付一覧の状態表示。
- 日付一括設定カレンダーの状態表示。
- ツールチップ内のコマ表示。
- 時数マネージャーの除外判定と理由。
- 起動時の表示日選択。

予定解決の `getResolvedSlot()`、期間別規則、日別変更は表示可否と分離したままにし、固定休業日を理由に内容を消したり空文字へ変換したりしない。

## 7. 既存挙動として維持するもの

- 日付個別の休日設定と日種別の保持・復活。
- `noclass-hide` と `noclass-show` の表示差および時数除外。
- 短縮、短縮AM、午前時程、考査、模擬試験の表示枠とチャイム種別。
- 朝、昼休み、放課後という名称。
- 土曜・日曜・祝日のカレンダー配色。
- `daySlotConfig` がオフのコマを通常日でも表示・時数対象にしない動作。
- 日別変更が基本時間割より優先される動作。
- 基本時間割の期間適用で日別変更を保持する動作。
- 休日、固定休業日、非表示コマであっても保存済み予定を削除しないこと。
- import前、schema移行前、基本時間割期間適用前の復旧snapshot安全策。
- undo/redoの履歴保存と設定画面表示中の編集保護。

## 8. テスト仕様

「テストが通った」だけでなく、次の受け入れ条件をすべて確認する。

### 8.1 core自動テスト

1. `defaultInstructionDayConfig()` が月〜金true、土日false。
2. 0〜6各曜日の正規化。部分入力の補完。
3. 未知キー、非boolean、配列、nullを拒否。
4. schema 1、2、3、4の各入力がschema 5へ移行し、既定授業日設定を持つ。
5. schema 5の再読み込みが冪等。
6. schema 5 exportに `instructionDayConfig` が一度だけ含まれる。
7. 移行前snapshot成功時のみmasterを書き換える。
8. snapshot失敗時にraw masterを保持し、既存どおり読み取り専用になる。
9. 日〜土すべてで曜日番号が正しく解決される。特に日曜0、土曜6を個別検証する。
10. 固定休業日＋通常、固定休業日＋各日種別、固定休業日＋休日＋各日種別の判定表を網羅する。
11. 日種別解除後に休日または固定休業日が復活する。
12. 固定休業日に1〜7限が非表示・時数除外となり、朝・昼休み・放課後は `daySlotConfig` に従う。
13. 授業日に戻す前後で `daySlotConfig`、`weeklyTemplate`、`weeklyRules`、`dateOverrides` が同一。
14. invalid import、将来schema、未知フィールドを既存どおり拒否し、masterを変更しない。

既存のcoreテストは削除・弱体化しない。schema番号を更新するだけで意味が失われる期待値は、schema 5の同等条件へ更新する。

### 8.2 UI・操作確認

1. 月〜日の授業日チェック、保存、再読み込み、再起動後の保持。
2. 固定休業日で1〜7限とチャイムが消え、朝・昼休み・放課後が個別設定どおり残る。
3. 固定休業日から授業日に戻すと、以前の1〜7限設定と予定が復元。
4. 土曜日を授業日にすると設定済みの土曜コマとチャイムを表示。
5. 平日を固定休業日にすると起動時候補から除外。
6. 固定休業日に短縮、短縮AM、午前時程、考査、模擬試験を設定すると、その日だけ日種別が優先。
7. 上記日種別を解除すると固定休業日へ復帰。
8. 固定休業日かつ休日で日種別を設定・解除し、休日と固定曜日設定が消えない。
9. `noclass-hide` と `noclass-show` は授業日・固定休業日の双方で既存の表示・チャイム・時数動作を維持。
10. ツールチップ、メイン、サブ、時数マネージャーで判定が一致。
11. 基本時間割から重複チェックボックスがなく、セクション2の変更がセル状態へ反映。
12. `daySlotConfig` 無効セルに既存値を入れた状態で基本時間割を保存しても値が消えない。
13. 固定休業日の列でも基本時間割を編集・保存・期間適用できる。
14. 設定セクション2を保存しても画面が閉じず、他セクションの未保存入力が残る。
15. 未保存で戻ると確認し、キャンセルで入力保持、破棄で再表示時に保存済み値へ戻る。
16. 保存失敗時にフォームとappStateを成功扱いにせず、既存データを保持。
17. undo/redoで授業日設定変更を戻す・やり直す。
18. キーボード操作、フォーカス表示、ラベル読み上げが成立。

### 8.3 画面サイズ

少なくとも次を実ブラウザで確認する。

- 1440×900
- 1024×768
- 768×1024

確認項目:

- 表の列が押し潰されない。
- 必要な場合だけ表内で横スクロールできる。
- チャイム表が不自然な幅にならない。
- 保存ボタン、授業日チェック、全反転ボタンが重ならない。
- 基本時間割の入力幅とキーボード移動が従来どおり使える。
- メイン画面の1列／2列表示に空列や極端に狭い予定枠が再発しない。

### 8.4 静的検証

- `node --check taskkanri-app.js`
- `node --check taskkanri-core.mjs`
- `node --test tests/core.test.mjs`
- `git diff --check`
- HTML内の `taskkanri-app.js` query、app内の `taskkanri-core.mjs` query、`APP_CONFIG.buildVersion` を同じ新build IDへ更新する。

## 9. 実装順序

1. 現在の既存挙動を表すcharacterization testを追加する。
2. schema 5、既定値、validator、migration、純粋判定関数をcoreへ追加する。
3. coreテストを完了させる。
4. メイン表示、ツールチップ、時数、起動日の土日固定判定を共通関数へ置換する。
5. セクション2の授業日UIと明示的な全反転操作を実装する。
6. セクション3の重複チェックを除去し、無効セル保存時のデータ保持を実装する。
7. 設定画面の未保存管理と離脱確認を実装する。
8. 全自動テストと3画面サイズのブラウザ確認を行う。
9. build IDとmodule queryを同期し、差分を再確認する。
10. 実装承認に公開が含まれる場合だけcommit、push、GitHub Pagesの反映を確認する。

## 10. 変更対象の目安

- `taskkanri-core.mjs`
  - schema、state、正規化、移行、曜日別授業日設定、日付表示方針の純粋関数。
- `taskkanri-app.js`
  - メイン／サブ表示、ツールチップ、時数、起動日、状態表示、設定表、保存、未保存管理。
- `index.html`
  - 設定セクションの文言・構造・CSS、module query。
- `tests/core.test.mjs`
  - schema 5、判定表、データ保持、安全な移行。
- 必要に応じてbrowserテストを追加するが、既存のcoreテストだけでUI確認済みとは扱わない。

## 11. 対象外

- 日付ごとの新しい「臨時授業日」フラグの追加。今回は既存の日種別が固定休業日を上書きする。
- 曜日ごとに異なるチャイム時刻セットを持つ機能。
- 朝、昼休み、放課後の名称変更。
- 設定画面全体を単一の保存ボタン・単一draftへ作り替える大規模改修。
- カレンダーの土曜青・日曜祝日赤の変更。
- 基本時間割、日別変更、休日データの自動整理・削除。

## 12. 参考にした設計上の注意

- 曜日は「週末」の固定判定ではなく曜日配列として明示する: [FullCalendar businessHours](https://fullcalendar.io/docs/v4/businessHours)
- 親子関係を持つ選択にはチェックボックスを使い、色だけに依存しない: [Apple Human Interface Guidelines - Toggles](https://developer.apple.com/design/human-interface-guidelines/toggles?changes=_7)
- 関連するチェックボックスを視覚的・意味的にグループ化する: [W3C - Grouping Controls](https://www.w3.org/WAI/tutorials/forms/grouping/)
- 日曜／土曜の曜日番号と週開始設定の混同は実際にスケジュール欠落を起こし得るため、0と6を独立して回帰テストする: [Home Assistant frontend issue #13685](https://github.com/home-assistant/frontend/issues/13685)

## 13. 完了条件

以下をすべて満たした時だけ実装完了とする。

- 本仕様の受け入れテストがすべて成功。
- 既存自動テストがすべて成功し、テストを削除・緩和していない。
- 保存済みデータが固定休業日の切替や基本時間割保存で失われない。
- schema移行失敗時に既存rawが保持される。
- 3画面サイズで設定画面とメイン画面を確認済み。
- build IDとmodule queryが一致。
- 公開指示がある場合は、push成功、Pages workflow成功、cache-bustした公開資産のbuild/schemaを別々に確認済み。
