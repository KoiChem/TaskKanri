# TaskKanri 年度繰越・基本時間割主従整理 実装仕様（Terra向け）

## 1. 目的

次の3点を、既存データを不用意に失わない形で一体的に改善する。

1. メイン画面の各コマに出る「戻す」「以降」ボタンを上側の枠だけに表示する。
2. 「基本時間割」をマスター、「週間授業規則」を期間へ適用した従データとして、UIと処理を一貫させる。
3. 年度繰越時に、次年度の祝日を用意し、行事と基本時間割を新年度向けに初期化できるようにする。

この仕様は製品コードの現状（build `20260906-academic-rollover-v1`、schema 5、commit `edd5872`）を基準とする。

## 2. 用語と主従関係

### 2.1 基本時間割（主）

- 設定画面の曜日×時限の表。
- 新しい期間の時間割を作るためのマスター。
- 現行データでは `weeklyTemplate` が該当する。
- 「基本時間割を保存」はマスターだけを保存し、過去・現在の適用済み期間を遡って変更しない。

### 2.2 週間授業規則（従）

- 基本時間割を特定期間へ適用したスナップショット。
- 現行データでは `weeklyRules[weekday][slot][] = { from, to, content }` が該当する。
- 日付ごとの表示判定では週間授業規則を参照する。年度途中の時間割変更や過去の表示を保持するため、適用後はマスターから独立した確定記録とする。
- `dateOverrides` は従来どおり週間授業規則より優先する。

### 2.3 必須の不変条件

- マスターの保存だけで適用済み規則を変更しない。
- 「期間へ適用」で、その時点のマスター内容を週間授業規則へコピーする。
- 適用後にマスターを編集しても、過去期間の週間授業規則は変わらない。
- UI文言は「下書き」より「マスター」「期間へ適用」を優先し、主従関係を明示する。

## 3. メイン画面の「戻す」「以降」ボタン

ここでいう「戻す」「以降」は、共通Undo/Redoではなく、日別変更のある各コマの右側に表示される2ボタンを指す。

### 3.1 表示仕様

- 上側の枠（`top-panel`）だけに「戻す」「以降」を表示する。
- 下側2枠（`bottom-left-panel`、`bottom-right-panel`）には表示しない。
- 日別変更を示す「変」マークは3枠すべてに残す。
- 下側枠でも予定本文の編集はこれまでどおり可能とする。
- 下側の日付を日付一覧から上側へ開けば、「戻す」「以降」を操作できる。
- ボタンを非表示にした行へ空白や不自然な余白を残さない。

### 3.2 実装指針

- `createSlotEditor()` に `showRuleActions` 相当の明示的な引数を追加する。
- `renderEditor()` から `targetPosition === 'top'` の場合だけ真を渡す。
- `hasOverride`、由来表示、日別編集、`restoreBaseSlot()`、`changeFollowingWeekly()` の処理自体は変更しない。

## 4. 祝日データの整理

### 4.1 現状の問題

現行の `customHolidays` は、国民の祝日・振替休日と学校独自の休日を同じ文字列マップで保持している。このまま年度繰越で一括置換すると、学校独自休日を国民の祝日と誤認して削除する危険がある。

### 4.2 推奨データ構造

schemaを6へ更新し、少なくとも次を分離する。

```js
calendarHolidays: {
  "2027-04-29": { name: "昭和の日", status: "confirmed" }
},
customHolidays: {
  "2027-08-13": "学校閉庁日"
}
```

- `calendarHolidays`: 国民の祝日、振替休日、国民の休日など暦上の休日。
- `customHolidays`: ユーザーが登録した学校独自休日。
- 同じ日付に両方ある場合、休日判定は真とし、表示名は学校独自休日を優先する。必要ならtooltipに両方を表示する。
- 既存の休日によるコマ非表示、チャイム非表示、時数集計除外は、両マップの和集合に対して従来どおり動作させる。

### 4.3 schema 5から6への移行

- 既存 `customHolidays` の各項目を、同梱する確定祝日データと日付・名称が一致する場合だけ `calendarHolidays` へ移す。
- 一致しない項目、名称を編集した項目、判定不能な項目は `customHolidays` に残す。誤って消すより重複を許容する。
- schema移行前snapshot、正規化、検証、保存失敗時の読み取り専用化など、既存の安全処理を必ず通す。
- export/importはschema 6を正規形式とし、schema 1～5の既存データを引き続き受け入れる。

### 4.4 祝日データの供給

- 実行時の外部API/CDNを必須にしない。通信不能でもアプリを起動・利用できること。
- 内閣府の `syukujitsu.csv` を根拠とする確定データをローカルに同梱する。
- 確定データの範囲外は、祝日法ルールによる計算値を `tentative` として扱い、確定値と区別する。
- wizardの確認画面に「確定 N件 / 暫定 N件」を表示する。暫定がある場合は警告するが、繰越自体は可能とする。
- 後日確定データが更新された場合、ユーザー独自休日を壊さず `calendarHolidays` だけ更新できる構造にする。

参考資料:

- 内閣府「国民の祝日について」: https://www8.cao.go.jp/chosei/shukujitsu/gaiyou.html
- 公式データを内蔵し、範囲外をルール計算する設計例: https://github.com/tomatomerde/japan-calendar

## 5. 年度繰越wizard

### 5.1 基本動作

- 繰越元は現在年度、繰越先は必ず次年度。
- 対象期間は次年度の4月1日～翌年3月31日。
- 実行前に復旧snapshotを作成する。
- すべての変更を共通Undoの1操作として登録する。
- plan生成は純粋関数とし、確認・キャンセルではstateを変更しない。
- 保存失敗時はメモリ上のstateも元に戻し、成功表示を出さない。

### 5.2 基本時間割・週間授業規則

wizardのチェック項目を次の意味へ統一する。

```text
[ ] 基本時間割と週間授業規則を次年度へコピーする
```

- wizardを開くたび必ずOFFへ戻す。保存済みの選択状態を復元しない。

OFFの場合:

- `weeklyTemplate` を空にする。
- 繰越先年度の期間と重なる `weeklyRules` を安全に削除する。
- 繰越先期間をまたぐ規則は期間外部分を分割して保持する。
- 繰越元年度以前の規則は保持する。

ONの場合:

- 繰越元の `weeklyTemplate` を次年度用マスターとして保持する。
- 繰越先年度と重なる既存の週間授業規則をいったん対象外にしたうえで、繰越元年度の規則を1年移動して作成する。
- うるう日など単純な1年移動が不可能な境界は黙って補正せず、確認画面でエラーにする。
- マスターと適用済み規則の片方だけをコピーする状態を作らない。

重要: この仕様では「基本時間割をリセットする」という合意を主従関係にも反映するため、OFF時は繰越先の既存週間規則も削除対象となる。既存データがある場合は削除件数を強調表示し、追加確認を要求する。以前の「繰越先規則を常に保持する」挙動からの変更である。

### 5.3 年間行事

- `configEvents` のうち、繰越先年度の期間内にある項目を削除する。
- 繰越元年度とそれ以前の行事は履歴として保持する。
- 元年度の行事を日付だけ1年移動してコピーしない。
- 繰越先に事前登録済みの行事がある場合、削除件数を確認画面に表示する。
- 設定画面の年間行事欄は現在年度分を中心に編集し、保存時に他年度の項目を巻き込んで消さないよう、年度範囲でmergeする。

### 5.4 祝日・学校独自休日

- 繰越先年度の `calendarHolidays` を、対象年度の確定・暫定祝日データで置換する。
- 繰越元年度の `calendarHolidays` は履歴として保持する。
- 元年度の `customHolidays` は次年度へコピーしない。
- 繰越先に事前登録済みの `customHolidays` はユーザーデータとして保持する。
- 設定画面では、暦上の祝日は原則読み取り専用、学校独自休日は編集可能とする。

### 5.5 その他のデータ

- `dateOverrides`: 元年度からコピーしない。繰越先に既存のものがあれば保持する。
- `dayProfiles`: 元年度からコピーしない。繰越先に既存のものがあれば保持する。
- `countDateRange`: 次年度4月1日～翌年3月31日に設定する。
- `countSettings` と開始番号: 保持する。
- `timeConfig`、`daySlotConfig`、`instructionDayConfig`: 保持する。
- `globalTaskData`: 保持する。
- レイアウト、時計、スリープ防止、選択モード: 保持する。

## 6. wizard確認画面

実行前に最低限、次を表示する。

- 繰越元年度と繰越先年度。
- 基本時間割マスター: コピー／空にする。
- 週間授業規則: 作成件数、削除件数、分割件数。
- 年間行事: 繰越先で削除する件数。
- 暦上の祝日: 確定件数、暫定件数、対象期間。
- 学校独自休日: 元年度からコピーしないこと、繰越先既存分の保持件数。
- 日別変更・日別状態: コピーしないこと、繰越先既存分を保持すること。
- 時数集計期間。
- 復旧snapshotと共通Undoで戻せること。

繰越先に削除対象の基本時間割規則または行事が1件以上ある場合は、通常の実行ボタンとは別に確認チェックを要求する。

## 7. UI文言

- 設定見出し: `3. 基本時間割（マスター）`
- 保存ボタン: `基本時間割を保存`
- 適用見出し: `このマスターを期間へ適用`
- 適用ボタン: `指定期間の週間授業規則を作成`
- 補足: `適用後の週間授業規則は期間ごとの確定記録です。マスターを後から編集しても自動変更されません。`
- rolloverチェック: `基本時間割と週間授業規則を次年度へコピーする`

## 8. コアAPIの推奨形

既存API名を維持してもよいが、planの結果に処理内容を明示する。

```js
planAcademicYearRollover(state, {
  sourceYear,
  targetYear,
  copyBaseTimetable: false
})
```

planには最低限次を含める。

```js
{
  sourceYear,
  targetYear,
  copyBaseTimetable,
  nextWeeklyTemplate,
  nextWeeklyRules,
  nextConfigEvents,
  nextCalendarHolidays,
  counts: {
    removedTargetRules,
    copiedRules,
    splitRules,
    removedTargetEvents,
    confirmedHolidays,
    tentativeHolidays,
    preservedTargetCustomHolidays,
    preservedTargetOverrides,
    preservedTargetDayProfiles
  }
}
```

`applyAcademicYearRollover()` は、実行時のstateがplan作成時の前提と一致するか再確認する。可能ならstate fingerprintまたは対象データの比較を加え、古いplanの誤適用を防ぐ。

## 9. 必須テスト

### 9.1 単体テスト

- 基本時間割の保存だけでは週間授業規則が変化しない。
- 期間適用時にマスターの内容が週間授業規則へコピーされる。
- マスター編集後も過去期間の規則が変化しない。
- rollover既定値が毎回OFF。
- OFFでマスターが空になり、繰越先期間の規則だけが削除・分割される。
- ONでマスターと週間授業規則がセットでコピーされる。
- 元年度の規則・行事・祝日履歴が保持される。
- 繰越先の行事だけが削除される。
- 繰越先の暦上の祝日が設定される。
- 繰越先の学校独自休日が保持される。
- schema 5の公式相当祝日と独自休日が安全に分類される。
- 暫定祝日が確定祝日として誤表示されない。
- plan、キャンセル、失敗処理が元stateを変更しない。
- 繰越全体をUndo/Redoできる。

### 9.2 ブラウザ受入テスト

- 上側枠では「戻す」「以降」が表示される。
- 下側2枠では両ボタンが表示されず、「変」マークは残る。
- 下側枠の通常編集はできる。
- 1440×900、1024×768、768×1024で余白崩れや横スクロール悪化がない。
- wizardを閉じて開き直すとコピーがOFFに戻る。
- 削除対象ありの追加確認を通らず実行できない。
- 実行後、設定欄・メイン画面・時数集計で祝日判定が一致する。
- 実行直後の共通Undoで年度、時間割、行事、祝日を一括で戻せる。
- console errorがない。

### 9.3 静的・公開確認

- `node --check taskkanri-core.mjs`
- `node --check taskkanri-app.js`
- `node --test tests/core.test.mjs`
- `git diff --check`
- build ID、HTMLのmodule query、core import queryを同じ値へ更新する。
- 実装承認後にのみcommit・pushし、GitHub Actionsとcache-bust付きGitHub Pagesを確認する。

## 10. 非対象

- 元年度の行事を次年度へコピーする選択肢は追加しない。
- 日別変更・日別状態の自動コピーは追加しない。
- 外部APIが使えないと起動できない構成にしない。
- 基本時間割マスターの編集で適用済み過去規則を自動更新しない。
- 下側枠の予定編集機能や日付移動機能を削除しない。

## 11. 完了条件

- 基本時間割が概念上のマスター、週間授業規則が期間別の従データとしてUI・保存処理・テストで一貫している。
- 年度繰越の既定OFFでは次年度の時間割と行事が空になり、次年度の暦上の祝日が用意される。
- コピーONでは基本時間割と週間授業規則が矛盾なくセットで作られる。
- 過年度データ、次年度の学校独自休日、次年度の日別変更・日別状態が意図せず失われない。
- 上側枠だけに「戻す」「以降」が表示される。
- snapshot、Undo/Redo、import/export、schema移行、公開版を含む回帰確認が完了している。
